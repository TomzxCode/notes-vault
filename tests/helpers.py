"""Shared test constants and helpers."""

# Raw key values used in tests - import these in test files that need to call user commands
TEST_RAW_KEYS = {
    "admin_key": "test-admin-raw-key",
    "work_key": "test-work-raw-key",
    "public_key": "test-public-raw-key",
}
